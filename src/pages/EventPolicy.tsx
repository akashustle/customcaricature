import CmsPolicyPage from "@/components/CmsPolicyPage";
const EventPolicy = () => (
  <CmsPolicyPage
    slug="event-policy"
    fallbackTitle="Event Booking Policy"
    fallbackDescription="Event booking policy for live caricature artist bookings at weddings, corporate events & parties."
    canonical="/event-policy"
  />
);
export default EventPolicy;
